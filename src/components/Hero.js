import Heading from "./Heading"
import "../index.css"

const Hero = () => {
    return (
        <div className="hero">
            <Heading />
            <h3>We have robots for the robot work.</h3>
            <button>a button</button>
        </div>
    )
}

export default Hero
