import Heading from "./Heading"
import "../index.css"

const Hero = () => {
    return (
        <div className="hero">
            <Heading />
            <p>Some text</p>
            <button>a button</button>
        </div>
    )
}

export default Hero
