import Heading from "./Heading"
import "../index.css"

const Hero = () => {
    return (
        <div className="hero">
            <Heading />
            <p>We have robots for the robot work.</p>
            <button>a button</button>
        </div>
    )
}

export default Hero
